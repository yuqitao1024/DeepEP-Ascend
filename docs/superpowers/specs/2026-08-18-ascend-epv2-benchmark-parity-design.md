# Ascend EPv2 Benchmark Parity Design

**Date:** 2026-08-18

**Status:** Implemented for synchronous BF16/FP8 performance cases and BF16 functional cases

## Purpose

Add a standalone Ascend EPv2 benchmark suite while preserving a workload-parity
profile for `tests/elastic/test_ep.py`. The benchmark compares DeepEP on CUDA
GPUs and Ascend 950 using identical rank counts, tensor shapes, routing
manifests, operation variants, warmup counts, sample counts, and logical-byte
formulas.

Payload values do not need to be bitwise identical across CUDA and NPU. Payload
dtype, shape, transferred bytes, routing decisions, per-rank send and receive
counts, top-k weights, bias count, alignment, and masking must match.

## Reuse Requirement

The Ascend suite must reuse the behavior currently implemented by
`tests/elastic/test_ep.py`; it must not create an independent copy of the EP
benchmark algorithm. Backend-neutral behavior is extracted into shared modules
under `tests/utils`, and both the existing CUDA entry point and the new Ascend
entry point call those modules.

The single shared implementation owns:

- `enumerate_ep_modes()` and stable case expansion;
- deterministic routing, weights, bias shapes, and workload summaries;
- normal, expanded, cached, and cached-expanded dispatch preparation;
- normal and reduced/expanded combine preparation;
- correctness reference construction and result checks;
- dispatch/combine logical-byte and bandwidth formulas; and
- the canonical warmup, measurement, and statistic protocol.

Backend-specific code owns distributed initialization, device tensor
materialization, event/profiler adapters, capability classification, and
resource reporting. The CUDA parity path currently reuses the Ascend benchmark's
report and runtime orchestration adapters in addition to the backend-neutral
modules. Refactoring `tests/elastic/test_ep.py` must preserve its default CLI and
behavior when parity options are absent.

## Scope

### Included

- EPv2 `ElasticBuffer` barrier, dispatch, and combine.
- The complete 144-mode matrix generated by `enumerate_ep_modes()`.
- Explicit capability classification for every mode.
- Measurements for the CUDA/Ascend intersection: synchronous BF16/FP8
  performance modes and BF16 event/stream functional modes, all with BF16
  combine.
- Normal, expanded, cached dispatch, normal combine, and reduced/expanded combine.
- Correctness setup for cached expanded dispatch with zero padding.
- Deterministic workload manifests shared by the CUDA parity profile and Ascend.
- Terminal tables and machine-readable JSON.
- Two-, four-, and eight-rank single-host scale-up execution.

### Excluded

- Implementing deferred DeepEP operators or transport capabilities.
- FP8 async events, communication streams, or compute overlap implementation.
- Hybrid routing or cross-host scale-out implementation.
- PP, Engram, AGRS, all-gather, legacy V1, or CP benchmarks.
- Declaring CUDA and Ascend kernel-stage timings directly comparable when their
  internal kernel decompositions differ.

## Suite Boundary

The upstream loop couples correctness checks and timing across one 144-mode
matrix. The Ascend adaptation separates intent from capability:

- the performance suite contains only synchronous modes without previous
  events, asynchronous compute overlap, or communication-stream allocation;
- the performance suite remains the 24 synchronous BF16 and FP8 modes;
- the functional suite contains 120 event/stream modes, of which all 60 BF16
  rows are supported after Phase 3E.2 and all 60 FP8 rows remain deferred;
- the default CUDA/Ascend supported intersection is therefore 84 modes; and
- every timed case retains a correctness preflight, but that preflight is a
  measurement gate rather than a separate functional case.

The complete 144-mode inventory remains visible through case listing and in
this SPEC. Reports contain only selected supported cases; their case IDs and
mode fields preserve the inputs used to derive performance or functional suite
classification. Deferred FP8 functional modes cannot be mistaken for measured
records.

| Capability | CUDA parity profile | Ascend 950 | Comparison treatment |
| --- | --- | --- | --- |
| BF16 payload | Supported | Supported | Comparable |
| Synchronous FP8 dispatch | Supported | Supported | Comparable |
| Previous event chaining | Supported | BF16 supported; FP8 deferred | Functional suite; BF16 comparable |
| Async compute-stream overlap | Supported | BF16 supported; FP8 deferred | Functional suite; BF16 comparable |
| Communication-stream allocation | Supported | BF16 supported; FP8 deferred | Functional suite; BF16 comparable |
| `allow_multiple_reduction=0/1` | Supported | Supported | Comparable |
| Masked routes (`topk_idx=-1`) | Supported | Supported | Comparable |
| Balanced/unbalanced routing | Supported | Supported | Comparable |
| Deterministic repeat | Supported | Supported | Comparable |
| Full `do_cpu_sync=0` flow | Supported | Unsupported | Listed, not measured on Ascend |
| Hybrid routing | Supported | Unsupported | Outside comparable profile |
| Cross-host scale-out | Supported | Unsupported | Outside comparable profile |
| Automatic SM/QP tuning | Supported | Unsupported | Record CUDA auto and Ascend fixed `1/0` |

Ascend uses `num_sms=1` and `num_qps=0`. The zero QP value means the CUDA QP
argument is unused; Ascend communication still uses HCOMM/URMA resources.

FP8 expanded and cached-expanded dispatch set
`use_tma_aligned_col_major_sf=True` on both backends, matching the upstream
column-major scale-factor layout. Upstream also passes `True` for BF16, where
the flag has no semantic effect because there are no scale factors. The Ascend
public API rejects that meaningless BF16 combination, so its parity adapter
passes `False`; BF16 payload bytes, routing, and measured operations are
unchanged.

The FP8 benchmark materializes contiguous E4M3 payloads and contiguous FP32
scale factors, one factor per 128 hidden values. The hidden width must therefore
be divisible by 128. FP8 is the dispatch wire and expert-input format; combine
continues to consume the prepared BF16 payload and produce BF16 output.

## Case Model

### Axes

| Axis | Values | Cardinality |
| --- | --- | ---: |
| Handle copy | `1`, `0` | 2 |
| Expert alignment | `128`, `1` | 2 |
| Dispatch dtype | `fp8`, `bf16` | 2 |
| Bias count | `0`, `1`, `2` | 3 |
| Previous event | `0`, `1` | 2 |
| Async compute stream | `0`, `1` | 2 |
| Allocate on comm stream | previous=0: `0`,`1`; previous=1: `1` | conditional |

The conditional allocation axis produces six stream/event combinations for
each handle/alignment/dtype/bias tuple, for exactly 144 cases.

### Stable IDs

Case IDs use:

```text
ep-{dtype}-align{alignment}-bias{bias}-hcopy{copy}-prev{previous}-async{async}-alloc{allocate}
```

### Suite Summary

| Suite classification | Count | Current Ascend treatment | Stable reason |
| --- | ---: | --- | --- |
| Current BF16 performance | 12 | Correctness preflight and timing | empty |
| Current FP8 performance | 12 | Correctness preflight and timing | empty |
| Current BF16 functional | 60 | Correctness preflight and timing | empty |
| Deferred FP8 functional | 60 | Listed, not executed | `fp8_full_row_deferred_3f` |
| Total | 144 | 84 supported / 60 deferred | |

BF16 functional rows retain their functional classification after becoming
supported; support status does not turn them into performance-suite cases.
FP8 functional rows use one stable reason because Phase 3F supports synchronous
FP8 dispatch but does not qualify the complete event/stream row. JSON records
every requested feature flag, so the specific previous/async/allocation
combination remains explicit.

### Exhaustive Case Table

| # | Case ID | Suite | Ascend status | Reason |
| ---: | --- | --- | --- | --- |
| 1 | `ep-fp8-align128-bias0-hcopy1-prev0-async0-alloc0` | Performance | Supported |  |
| 2 | `ep-fp8-align128-bias0-hcopy1-prev0-async0-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 3 | `ep-fp8-align128-bias0-hcopy1-prev0-async1-alloc0` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 4 | `ep-fp8-align128-bias0-hcopy1-prev0-async1-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 5 | `ep-fp8-align128-bias0-hcopy1-prev1-async0-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 6 | `ep-fp8-align128-bias0-hcopy1-prev1-async1-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 7 | `ep-fp8-align128-bias1-hcopy1-prev0-async0-alloc0` | Performance | Supported |  |
| 8 | `ep-fp8-align128-bias1-hcopy1-prev0-async0-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 9 | `ep-fp8-align128-bias1-hcopy1-prev0-async1-alloc0` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 10 | `ep-fp8-align128-bias1-hcopy1-prev0-async1-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 11 | `ep-fp8-align128-bias1-hcopy1-prev1-async0-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 12 | `ep-fp8-align128-bias1-hcopy1-prev1-async1-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 13 | `ep-fp8-align128-bias2-hcopy1-prev0-async0-alloc0` | Performance | Supported |  |
| 14 | `ep-fp8-align128-bias2-hcopy1-prev0-async0-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 15 | `ep-fp8-align128-bias2-hcopy1-prev0-async1-alloc0` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 16 | `ep-fp8-align128-bias2-hcopy1-prev0-async1-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 17 | `ep-fp8-align128-bias2-hcopy1-prev1-async0-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 18 | `ep-fp8-align128-bias2-hcopy1-prev1-async1-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 19 | `ep-bf16-align128-bias0-hcopy1-prev0-async0-alloc0` | Performance | Supported |  |
| 20 | `ep-bf16-align128-bias0-hcopy1-prev0-async0-alloc1` | Functional | Supported |  |
| 21 | `ep-bf16-align128-bias0-hcopy1-prev0-async1-alloc0` | Functional | Supported |  |
| 22 | `ep-bf16-align128-bias0-hcopy1-prev0-async1-alloc1` | Functional | Supported |  |
| 23 | `ep-bf16-align128-bias0-hcopy1-prev1-async0-alloc1` | Functional | Supported |  |
| 24 | `ep-bf16-align128-bias0-hcopy1-prev1-async1-alloc1` | Functional | Supported |  |
| 25 | `ep-bf16-align128-bias1-hcopy1-prev0-async0-alloc0` | Performance | Supported |  |
| 26 | `ep-bf16-align128-bias1-hcopy1-prev0-async0-alloc1` | Functional | Supported |  |
| 27 | `ep-bf16-align128-bias1-hcopy1-prev0-async1-alloc0` | Functional | Supported |  |
| 28 | `ep-bf16-align128-bias1-hcopy1-prev0-async1-alloc1` | Functional | Supported |  |
| 29 | `ep-bf16-align128-bias1-hcopy1-prev1-async0-alloc1` | Functional | Supported |  |
| 30 | `ep-bf16-align128-bias1-hcopy1-prev1-async1-alloc1` | Functional | Supported |  |
| 31 | `ep-bf16-align128-bias2-hcopy1-prev0-async0-alloc0` | Performance | Supported |  |
| 32 | `ep-bf16-align128-bias2-hcopy1-prev0-async0-alloc1` | Functional | Supported |  |
| 33 | `ep-bf16-align128-bias2-hcopy1-prev0-async1-alloc0` | Functional | Supported |  |
| 34 | `ep-bf16-align128-bias2-hcopy1-prev0-async1-alloc1` | Functional | Supported |  |
| 35 | `ep-bf16-align128-bias2-hcopy1-prev1-async0-alloc1` | Functional | Supported |  |
| 36 | `ep-bf16-align128-bias2-hcopy1-prev1-async1-alloc1` | Functional | Supported |  |
| 37 | `ep-fp8-align1-bias0-hcopy1-prev0-async0-alloc0` | Performance | Supported |  |
| 38 | `ep-fp8-align1-bias0-hcopy1-prev0-async0-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 39 | `ep-fp8-align1-bias0-hcopy1-prev0-async1-alloc0` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 40 | `ep-fp8-align1-bias0-hcopy1-prev0-async1-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 41 | `ep-fp8-align1-bias0-hcopy1-prev1-async0-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 42 | `ep-fp8-align1-bias0-hcopy1-prev1-async1-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 43 | `ep-fp8-align1-bias1-hcopy1-prev0-async0-alloc0` | Performance | Supported |  |
| 44 | `ep-fp8-align1-bias1-hcopy1-prev0-async0-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 45 | `ep-fp8-align1-bias1-hcopy1-prev0-async1-alloc0` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 46 | `ep-fp8-align1-bias1-hcopy1-prev0-async1-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 47 | `ep-fp8-align1-bias1-hcopy1-prev1-async0-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 48 | `ep-fp8-align1-bias1-hcopy1-prev1-async1-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 49 | `ep-fp8-align1-bias2-hcopy1-prev0-async0-alloc0` | Performance | Supported |  |
| 50 | `ep-fp8-align1-bias2-hcopy1-prev0-async0-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 51 | `ep-fp8-align1-bias2-hcopy1-prev0-async1-alloc0` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 52 | `ep-fp8-align1-bias2-hcopy1-prev0-async1-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 53 | `ep-fp8-align1-bias2-hcopy1-prev1-async0-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 54 | `ep-fp8-align1-bias2-hcopy1-prev1-async1-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 55 | `ep-bf16-align1-bias0-hcopy1-prev0-async0-alloc0` | Performance | Supported |  |
| 56 | `ep-bf16-align1-bias0-hcopy1-prev0-async0-alloc1` | Functional | Supported |  |
| 57 | `ep-bf16-align1-bias0-hcopy1-prev0-async1-alloc0` | Functional | Supported |  |
| 58 | `ep-bf16-align1-bias0-hcopy1-prev0-async1-alloc1` | Functional | Supported |  |
| 59 | `ep-bf16-align1-bias0-hcopy1-prev1-async0-alloc1` | Functional | Supported |  |
| 60 | `ep-bf16-align1-bias0-hcopy1-prev1-async1-alloc1` | Functional | Supported |  |
| 61 | `ep-bf16-align1-bias1-hcopy1-prev0-async0-alloc0` | Performance | Supported |  |
| 62 | `ep-bf16-align1-bias1-hcopy1-prev0-async0-alloc1` | Functional | Supported |  |
| 63 | `ep-bf16-align1-bias1-hcopy1-prev0-async1-alloc0` | Functional | Supported |  |
| 64 | `ep-bf16-align1-bias1-hcopy1-prev0-async1-alloc1` | Functional | Supported |  |
| 65 | `ep-bf16-align1-bias1-hcopy1-prev1-async0-alloc1` | Functional | Supported |  |
| 66 | `ep-bf16-align1-bias1-hcopy1-prev1-async1-alloc1` | Functional | Supported |  |
| 67 | `ep-bf16-align1-bias2-hcopy1-prev0-async0-alloc0` | Performance | Supported |  |
| 68 | `ep-bf16-align1-bias2-hcopy1-prev0-async0-alloc1` | Functional | Supported |  |
| 69 | `ep-bf16-align1-bias2-hcopy1-prev0-async1-alloc0` | Functional | Supported |  |
| 70 | `ep-bf16-align1-bias2-hcopy1-prev0-async1-alloc1` | Functional | Supported |  |
| 71 | `ep-bf16-align1-bias2-hcopy1-prev1-async0-alloc1` | Functional | Supported |  |
| 72 | `ep-bf16-align1-bias2-hcopy1-prev1-async1-alloc1` | Functional | Supported |  |
| 73 | `ep-fp8-align128-bias0-hcopy0-prev0-async0-alloc0` | Performance | Supported |  |
| 74 | `ep-fp8-align128-bias0-hcopy0-prev0-async0-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 75 | `ep-fp8-align128-bias0-hcopy0-prev0-async1-alloc0` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 76 | `ep-fp8-align128-bias0-hcopy0-prev0-async1-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 77 | `ep-fp8-align128-bias0-hcopy0-prev1-async0-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 78 | `ep-fp8-align128-bias0-hcopy0-prev1-async1-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 79 | `ep-fp8-align128-bias1-hcopy0-prev0-async0-alloc0` | Performance | Supported |  |
| 80 | `ep-fp8-align128-bias1-hcopy0-prev0-async0-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 81 | `ep-fp8-align128-bias1-hcopy0-prev0-async1-alloc0` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 82 | `ep-fp8-align128-bias1-hcopy0-prev0-async1-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 83 | `ep-fp8-align128-bias1-hcopy0-prev1-async0-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 84 | `ep-fp8-align128-bias1-hcopy0-prev1-async1-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 85 | `ep-fp8-align128-bias2-hcopy0-prev0-async0-alloc0` | Performance | Supported |  |
| 86 | `ep-fp8-align128-bias2-hcopy0-prev0-async0-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 87 | `ep-fp8-align128-bias2-hcopy0-prev0-async1-alloc0` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 88 | `ep-fp8-align128-bias2-hcopy0-prev0-async1-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 89 | `ep-fp8-align128-bias2-hcopy0-prev1-async0-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 90 | `ep-fp8-align128-bias2-hcopy0-prev1-async1-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 91 | `ep-bf16-align128-bias0-hcopy0-prev0-async0-alloc0` | Performance | Supported |  |
| 92 | `ep-bf16-align128-bias0-hcopy0-prev0-async0-alloc1` | Functional | Supported |  |
| 93 | `ep-bf16-align128-bias0-hcopy0-prev0-async1-alloc0` | Functional | Supported |  |
| 94 | `ep-bf16-align128-bias0-hcopy0-prev0-async1-alloc1` | Functional | Supported |  |
| 95 | `ep-bf16-align128-bias0-hcopy0-prev1-async0-alloc1` | Functional | Supported |  |
| 96 | `ep-bf16-align128-bias0-hcopy0-prev1-async1-alloc1` | Functional | Supported |  |
| 97 | `ep-bf16-align128-bias1-hcopy0-prev0-async0-alloc0` | Performance | Supported |  |
| 98 | `ep-bf16-align128-bias1-hcopy0-prev0-async0-alloc1` | Functional | Supported |  |
| 99 | `ep-bf16-align128-bias1-hcopy0-prev0-async1-alloc0` | Functional | Supported |  |
| 100 | `ep-bf16-align128-bias1-hcopy0-prev0-async1-alloc1` | Functional | Supported |  |
| 101 | `ep-bf16-align128-bias1-hcopy0-prev1-async0-alloc1` | Functional | Supported |  |
| 102 | `ep-bf16-align128-bias1-hcopy0-prev1-async1-alloc1` | Functional | Supported |  |
| 103 | `ep-bf16-align128-bias2-hcopy0-prev0-async0-alloc0` | Performance | Supported |  |
| 104 | `ep-bf16-align128-bias2-hcopy0-prev0-async0-alloc1` | Functional | Supported |  |
| 105 | `ep-bf16-align128-bias2-hcopy0-prev0-async1-alloc0` | Functional | Supported |  |
| 106 | `ep-bf16-align128-bias2-hcopy0-prev0-async1-alloc1` | Functional | Supported |  |
| 107 | `ep-bf16-align128-bias2-hcopy0-prev1-async0-alloc1` | Functional | Supported |  |
| 108 | `ep-bf16-align128-bias2-hcopy0-prev1-async1-alloc1` | Functional | Supported |  |
| 109 | `ep-fp8-align1-bias0-hcopy0-prev0-async0-alloc0` | Performance | Supported |  |
| 110 | `ep-fp8-align1-bias0-hcopy0-prev0-async0-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 111 | `ep-fp8-align1-bias0-hcopy0-prev0-async1-alloc0` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 112 | `ep-fp8-align1-bias0-hcopy0-prev0-async1-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 113 | `ep-fp8-align1-bias0-hcopy0-prev1-async0-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 114 | `ep-fp8-align1-bias0-hcopy0-prev1-async1-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 115 | `ep-fp8-align1-bias1-hcopy0-prev0-async0-alloc0` | Performance | Supported |  |
| 116 | `ep-fp8-align1-bias1-hcopy0-prev0-async0-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 117 | `ep-fp8-align1-bias1-hcopy0-prev0-async1-alloc0` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 118 | `ep-fp8-align1-bias1-hcopy0-prev0-async1-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 119 | `ep-fp8-align1-bias1-hcopy0-prev1-async0-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 120 | `ep-fp8-align1-bias1-hcopy0-prev1-async1-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 121 | `ep-fp8-align1-bias2-hcopy0-prev0-async0-alloc0` | Performance | Supported |  |
| 122 | `ep-fp8-align1-bias2-hcopy0-prev0-async0-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 123 | `ep-fp8-align1-bias2-hcopy0-prev0-async1-alloc0` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 124 | `ep-fp8-align1-bias2-hcopy0-prev0-async1-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 125 | `ep-fp8-align1-bias2-hcopy0-prev1-async0-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 126 | `ep-fp8-align1-bias2-hcopy0-prev1-async1-alloc1` | Functional | Deferred | `fp8_full_row_deferred_3f` |
| 127 | `ep-bf16-align1-bias0-hcopy0-prev0-async0-alloc0` | Performance | Supported |  |
| 128 | `ep-bf16-align1-bias0-hcopy0-prev0-async0-alloc1` | Functional | Supported |  |
| 129 | `ep-bf16-align1-bias0-hcopy0-prev0-async1-alloc0` | Functional | Supported |  |
| 130 | `ep-bf16-align1-bias0-hcopy0-prev0-async1-alloc1` | Functional | Supported |  |
| 131 | `ep-bf16-align1-bias0-hcopy0-prev1-async0-alloc1` | Functional | Supported |  |
| 132 | `ep-bf16-align1-bias0-hcopy0-prev1-async1-alloc1` | Functional | Supported |  |
| 133 | `ep-bf16-align1-bias1-hcopy0-prev0-async0-alloc0` | Performance | Supported |  |
| 134 | `ep-bf16-align1-bias1-hcopy0-prev0-async0-alloc1` | Functional | Supported |  |
| 135 | `ep-bf16-align1-bias1-hcopy0-prev0-async1-alloc0` | Functional | Supported |  |
| 136 | `ep-bf16-align1-bias1-hcopy0-prev0-async1-alloc1` | Functional | Supported |  |
| 137 | `ep-bf16-align1-bias1-hcopy0-prev1-async0-alloc1` | Functional | Supported |  |
| 138 | `ep-bf16-align1-bias1-hcopy0-prev1-async1-alloc1` | Functional | Supported |  |
| 139 | `ep-bf16-align1-bias2-hcopy0-prev0-async0-alloc0` | Performance | Supported |  |
| 140 | `ep-bf16-align1-bias2-hcopy0-prev0-async0-alloc1` | Functional | Supported |  |
| 141 | `ep-bf16-align1-bias2-hcopy0-prev0-async1-alloc0` | Functional | Supported |  |
| 142 | `ep-bf16-align1-bias2-hcopy0-prev0-async1-alloc1` | Functional | Supported |  |
| 143 | `ep-bf16-align1-bias2-hcopy0-prev1-async0-alloc1` | Functional | Supported |  |
| 144 | `ep-bf16-align1-bias2-hcopy0-prev1-async1-alloc1` | Functional | Supported |  |

### Operations Per Supported Case

| Operation | Correctness | Performance | Notes |
| --- | --- | --- | --- |
| Normal dispatch | Yes | Yes | Same routing manifest |
| Expanded dispatch | Yes | Yes | FP8 uses upstream column-major scales; Ascend leaves the meaningless BF16 scale-layout flag false |
| Cached dispatch | Yes | Yes | Reuses validated handle |
| Cached expanded zero padding | Yes | No | Upstream has no separate timing result |
| Normal combine | Yes | Yes | Same weights and bias count |
| Reduced/expanded combine | Yes | Yes | Same multiple-reduction setting |

The 84 supported modes each produce five primary operation records, for 420
records per default complete workload configuration. The 24 performance-suite
cases account for 120 of those records; the other 300 belong to the 60 supported
BF16 functional cases.

## Workload Parity

### Default Configuration

The parity default copies `tests/elastic/test_ep.py`:

| Parameter | Value |
| --- | --- |
| Local processes | 8 |
| Maximum tokens per rank | 4096 |
| Tokens on rank `r` | `max(1, 4096 - r)` |
| Hidden width | 7168 |
| Top-k | 6 |
| Experts | 256 |
| Seed | 0 |
| Unbalanced ratio | 1.0 |
| Precise unbalanced ratio | false |
| Masked ratio | 0.0 |
| Allow multiple reduction | true |
| Allow hybrid mode | false for comparable profile |
| CPU synchronization | true |

CLI overrides remain available for the same workload parameters.

### Manifest Contents

The CPU-side manifest stores or deterministically derives:

- schema version and seed;
- rank count and per-rank token counts;
- hidden, expert, top-k, alignment, dtype, and mask settings;
- exact `topk_idx` routing values;
- exact top-k weights when enabled;
- bias count and tensor shapes;
- per-rank and per-expert send/receive summaries;
- logical payload and metadata byte counts;
- a canonical SHA-256 workload fingerprint.

Large payload tensors and bias values are generated on each platform. Their
values may differ, but dtype, shape, and byte count must match. Routing and
weights must be identical because they determine traffic.

The existing CUDA test keeps its default random path unchanged. An opt-in
parity profile loads the shared manifest and emits the parity JSON schema.

## Timing Protocol

The canonical comparison metric is device elapsed time around the public
`ElasticBuffer` operation. CUDA uses CUDA events and Ascend uses NPU events.
Python wall time is also recorded to expose synchronous host overhead.

| Setting | Default |
| --- | ---: |
| Untimed preparation call | 1 |
| Warmup iterations | 30 |
| Measured iterations | 30 |
| Rank alignment | Before every measured sample, outside timing |
| Device synchronization | Before start and after end event |
| L2 flush | Disabled in canonical parity mode |

The 1+30 warmup shape matches the effective preparation and profiler warmup in
`bench_kineto`. L2 flush is disabled for both parity backends because the
current helper is CUDA-specific and there is no proven equivalent Ascend cache
flush contract.

Each timed operation record contains:

- raw device elapsed samples in seconds;
- raw wall-time samples in seconds;
- minimum, mean, p50, p95, and maximum;
- logical bytes and logical GB/s using decimal `1e9`;
- rank-local send and receive byte summaries;
- backend resource values, including CUDA SM/QP or Ascend fixed `1/0`;
- optional profiler kernel-stage diagnostics.

Kernel-stage diagnostics are not a comparison gate. CUDA main/copy epilogues
and Ascend AOT kernels may have different decomposition and names.

## Logical Bandwidth

Dispatch and combine byte calculations reuse the formulas in
`tests/elastic/test_ep.py`, including:

- scale-up receive bytes;
- scale-out send bytes when scale-out is supported;
- local-traffic inclusion or exclusion;
- dispatch payload, top-k index, and weight bytes;
- expanded metadata and expanded-token bytes;
- combine payload, bias, reduction-read, and reduction-write bytes.

Only records with matching workload fingerprint, case ID, operation ID,
logical-byte formula version, and world size may be compared.

## Components

| File | Responsibility |
| --- | --- |
| `tests/utils/ep_benchmark_core.py` | Shared operation sequence, correctness checks, logical-byte formulas, timing protocol |
| `tests/utils/ep_benchmark_manifest.py` | Shared case axes, manifest schema, deterministic routing, fingerprints |
| `tests/ascend/benchmark/workloads.py` | Ascend capability classification and operation expansion |
| `tests/ascend/benchmark/timing.py` | NPU event and wall-time sampling, statistics |
| `tests/ascend/benchmark/report.py` | JSON serialization and schema validation |
| `tests/ascend/benchmark/bench_ep.py` | HCCL setup, correctness, warmup, measurement, cleanup, CLI |
| `tests/ascend/benchmark/compare.py` | Match GPU/NPU records and produce comparison tables |
| `tests/ascend/benchmark/README.md` | Build, TaskQueue, torchrun, output, limitations |
| `tests/ascend/test_benchmark_contract.py` | Host-only matrix, manifest, report, statistics, CLI tests |
| `tests/elastic/test_ep.py` | Opt-in parity manifest/profile; existing default remains unchanged |

The shared core and manifest modules live under `tests/utils`. The CUDA parity
profile imports those modules and currently reuses the Ascend report, timing,
and matrix-orchestration adapters; the Ascend runner supplies NPU-specific
materialization and event timing.

## CLI Contract

Representative commands:

```bash
# Print all 144 cases without importing torch_npu
python3 tests/ascend/benchmark/bench_ep.py --list-cases --suite all

# Two-rank Ascend benchmark inside an allocated NPU task
torchrun --standalone --nproc-per-node=2 \
  tests/ascend/benchmark/bench_ep.py --output results/ascend-ep2.json

# GPU parity profile
python3 tests/elastic/test_ep.py --benchmark-profile parity \
  --workload-manifest results/ep8-workload.json \
  --benchmark-json results/cuda-ep8.json

# Compare matching records
python3 tests/ascend/benchmark/compare.py \
  results/cuda-ep8.json results/ascend-ep8.json
```

The `--suite` argument is valid with `--list-cases`; default benchmark execution
uses all 84 currently supported cases. Explicit `--cases` selection accepts
supported performance or functional cases and rejects deferred cases before
runtime dependency loading.

## Reporting

### Terminal

On success, rank 0 prints the output path and passed-case count after atomically
writing the complete JSON report. Runtime failures are raised through the
distributed launcher and do not produce a replacement report.

Case inventory is a separate listing operation. `--list-cases --suite all`
shows all 144 rows, `--suite performance` shows 12 BF16 and 12 FP8 rows, and
`--suite functional` shows 60 supported BF16 plus 60 deferred FP8 rows. Listing
does not initialize `torch_npu` or access an NPU.

### JSON

The top level contains:

```text
schema_version
formula_version
generated_at
git_commit
platform
device
world_size
workload
workload_fingerprint
timing_protocol
case_summary
cases
failures
```

`git_commit` records the repository `HEAD` that generated the report. Source
archives without Git metadata use the explicit value `unknown`.

The current runner emits JSON only after every selected case passes. Emitted
reports therefore have `case_summary.failed=0` and an empty `failures` list;
these schema fields are reserved for readers and future incremental reporting.
On failure, the process exits nonzero without replacing `--output`, so callers
must use the task exit status and report provenance rather than treating a
pre-existing file as the current run.

The report contains exactly the selected supported cases. The default complete
run therefore contains 84 passed cases and 420 operation records. Each row's
case ID and mode fields preserve its suite inputs. Deferred FP8 functional cases
appear only in inventory listings and cannot be added to a report.

## Error Handling

- Deferred cases are rejected from benchmark selection with their suite and
  stable reason.
- Every case in an emitted report is `passed`; a case failure aborts the run and
  prevents report replacement.
- A failure on any rank is aggregated before the next collective boundary.
- Peer failure produces a bounded, rank-qualified error.
- Correctness failure prevents timing for that case.
- Buffer destruction precedes process-group destruction.
- JSON is written atomically after all selected cases complete and rank results
  are aggregated.
- Comparison rejects mismatched fingerprints, case IDs, formula versions,
  world sizes, timing counts/aggregation, logical bytes, or operation IDs
  rather than displaying misleading ratios. CUDA and NPU event timer names are
  platform-specific and are not required to match.

## Testing

Host-only TDD coverage must prove:

- exactly 144 stable case IDs are generated;
- exactly 12 cases are BF16 performance, 12 are FP8 performance, 60 are
  supported BF16 functional, and 60 are deferred FP8 functional;
- every deferred FP8 functional case has reason `fp8_full_row_deferred_3f`;
- all performance cases are synchronous, event-free, and allocation-free;
- all supported functional cases are BF16 and cover every previous-event,
  async, and communication-allocation combination;
- each supported case expands to the required six correctness and five timed
  operations;
- identical manifest inputs produce identical fingerprints;
- route summaries and logical bytes match across CUDA and Ascend profiles;
- percentile and GB/s calculations are correct;
- default JSON contains exactly 84 passed cases and 420 operation records;
- comparison rejects incompatible inputs;
- all/performance/functional case listing works without importing `torch_npu`.

NPU acceptance must cover:

- two-rank full current-supported matrix;
- four-rank current-supported smoke;
- eight-rank current-supported smoke;
- bounded peer failure and cleanup;
- JSON production and comparison against a matching CUDA parity result.

Full four- and eight-rank correctness qualification is not inferred from smoke
coverage. Reports must record the qualification level.

## Acceptance Criteria

1. The standalone Ascend benchmark lists and classifies all 144 upstream modes.
2. The default report contains exactly 84 passed cases: 12 BF16 performance,
   12 FP8 performance, and 60 BF16 functional.
3. The inventory contains those 84 supported cases plus 60 deferred FP8
   functional cases with reasons matching this SPEC.
4. The 84 supported cases run the required operation variants and emit exactly
   420 timing records.
5. GPU and Ascend parity records share workload fingerprints and logical bytes.
6. Canonical latency and bandwidth statistics use the same timing protocol.
7. Existing `tests/elastic/test_ep.py` behavior is unchanged without parity flags.
8. CUDA and Ascend runners call the same shared mode, workload, operation,
   correctness, and logical-byte implementations.
9. Host-only tests and current Ascend platform/build contracts pass.
10. Two-rank NPU runtime acceptance passes before performance claims are made.
11. Results never imply support for FP8 combine, FP8 async, hybrid, or scale-out.
