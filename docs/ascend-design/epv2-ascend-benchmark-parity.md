# Ascend EPv2 Benchmark Parity Design

**Date:** 2026-08-18

**Status:** Implemented for all 144 synchronous and event/stream performance cases

## Purpose

Add a standalone Ascend EPv2 benchmark suite while preserving a workload-parity
profile for `tests/elastic/test_ep.py`. The benchmark compares DeepEP on CUDA
GPUs and Ascend 950 using identical rank counts, tensor shapes, routing
manifests, operation variants, warmup counts, sample counts, and logical-byte
formulas.

Payload values do not need to be bitwise identical across CUDA and NPU. Payload
dtype, shape, transferred bytes, routing decisions, per-rank send and receive
counts, top-k weights, bias count, alignment, and masking must match.

## Reuse Requirement

The Ascend suite must reuse the behavior currently implemented by
`tests/elastic/test_ep.py`; it must not create an independent copy of the EP
benchmark algorithm. Backend-neutral behavior is extracted into shared modules
under `tests/utils`, and both the existing CUDA entry point and the new Ascend
entry point call those modules.

The single shared implementation owns:

- `enumerate_ep_modes()` and stable case expansion;
- deterministic routing, weights, bias shapes, and workload summaries;
- normal, expanded, cached, and cached-expanded dispatch preparation;
- normal and reduced/expanded combine preparation;
- correctness reference construction and result checks;
- dispatch/combine logical-byte and bandwidth formulas; and
- the canonical warmup, measurement, and statistic protocol.

Backend-specific code owns distributed initialization, device tensor
materialization, event/profiler adapters, capability classification, and
resource reporting. The CUDA parity path currently reuses the Ascend benchmark's
report and runtime orchestration adapters in addition to the backend-neutral
modules. Refactoring `tests/elastic/test_ep.py` must preserve its default CLI and
behavior when parity options are absent.

## Scope

### Included

- EPv2 `ElasticBuffer` barrier, dispatch, and combine.
- The complete 144-mode matrix generated by `enumerate_ep_modes()`.
- Explicit capability classification for every mode.
- Measurements for all 144 synchronous and event/stream BF16/FP8 performance
  modes in the CUDA/Ascend intersection, all with BF16 combine.
- Normal, expanded, cached dispatch, normal combine, and reduced/expanded combine.
- Correctness setup for cached expanded dispatch with zero padding.
- Deterministic workload manifests shared by the CUDA parity profile and Ascend.
- Terminal tables and machine-readable JSON.
- Two-, four-, and eight-rank single-host scale-up execution.

### Excluded

- Implementing transport capabilities outside the comparable profile.
- Hybrid routing or cross-host scale-out implementation.
- PP, Engram, AGRS, all-gather, legacy V1, or CP benchmarks.
- Declaring CUDA and Ascend kernel-stage timings directly comparable when their
  internal kernel decompositions differ.

## Suite Boundary

The upstream loop couples correctness checks and timing across one 144-mode
matrix. The Ascend adaptation separates intent from capability:

- the performance suite contains 24 synchronous BF16/FP8 baseline modes and
  120 BF16/FP8 event/stream modes, with the BF16 rows promoted in Phase 3E.2
  and the FP8 rows promoted in Phase 3F;
- the functional suite is empty because all 144 modes are supported;
- the default CUDA/Ascend supported intersection is therefore 144 modes; and
- every timed case retains a correctness preflight, but that preflight is a
  measurement gate rather than a separate functional case.

The complete 144-mode inventory remains visible through case listing and in
this SPEC. Every row is currently a supported performance case with an empty
reason. Reports contain only selected supported cases; their case IDs and mode
fields preserve all upstream event/stream inputs.

| Capability | CUDA parity profile | Ascend 950 | Comparison treatment |
| --- | --- | --- | --- |
| BF16 payload | Supported | Supported | Comparable |
| Synchronous FP8 dispatch | Supported | Supported | Comparable |
| Previous event chaining | Supported | Supported | Comparable for BF16 and FP8 |
| Async compute-stream overlap | Supported | Supported | Comparable for BF16 and FP8 |
| Communication-stream allocation | Supported | Supported | Comparable for BF16 and FP8 |
| `allow_multiple_reduction=0/1` | Supported | Supported | Comparable |
| Masked routes (`topk_idx=-1`) | Supported | Supported | Comparable |
| Balanced/unbalanced routing | Supported | Supported | Comparable |
| Deterministic repeat | Supported | Supported | Comparable |
| Full `do_cpu_sync=0` flow | Supported | Unsupported | Listed, not measured on Ascend |
| Hybrid routing | Supported | Unsupported | Outside comparable profile |
| Cross-host scale-out | Supported | Unsupported | Outside comparable profile |
| Automatic SM/QP tuning | Supported | Unsupported | Record CUDA auto and Ascend fixed `1/0` |

Ascend uses `num_sms=1` and `num_qps=0`. The zero QP value means the CUDA QP
argument is unused; Ascend communication still uses HCOMM/URMA resources.

FP8 expanded and cached-expanded dispatch set
`use_tma_aligned_col_major_sf=True` on both backends, matching the upstream
column-major scale-factor layout. Upstream also passes `True` for BF16, where
the flag has no semantic effect because there are no scale factors. The Ascend
public API rejects that meaningless BF16 combination, so its parity adapter
passes `False`; BF16 payload bytes, routing, and measured operations are
unchanged.

The FP8 benchmark materializes contiguous E4M3 payloads and contiguous FP32
scale factors, one factor per 128 hidden values. The hidden width must therefore
be divisible by 128. FP8 is the dispatch wire and expert-input format; combine
continues to consume the prepared BF16 payload and produce BF16 output.

## Case Model

### Axes

| Axis | Values | Cardinality |
| --- | --- | ---: |
| Handle copy | `1`, `0` | 2 |
| Expert alignment | `128`, `1` | 2 |
| Dispatch dtype | `fp8`, `bf16` | 2 |
| Bias count | `0`, `1`, `2` | 3 |
| Previous event | `0`, `1` | 2 |
| Async compute stream | `0`, `1` | 2 |
| Allocate on comm stream | previous=0: `0`,`1`; previous=1: `1` | conditional |

The conditional allocation axis produces six stream/event combinations for
each handle/alignment/dtype/bias tuple, for exactly 144 cases.

### Stable IDs

Case IDs use:

```text
ep-{dtype}-align{alignment}-bias{bias}-hcopy{copy}-prev{previous}-async{async}-alloc{allocate}
```

### Suite Summary

| Suite classification | Count | Current Ascend treatment | Stable reason |
| --- | ---: | --- | --- |
| Synchronous baseline performance | 24 | Correctness preflight and timing | empty |
| BF16 async/event performance | 60 | Correctness preflight and timing | empty |
| FP8 async/event performance | 60 | Correctness preflight and timing | empty |
| Total | 144 | 144 supported | |

The backend-neutral manifest still identifies event/stream shapes separately
from synchronous shapes. Phase 3E.2 promoted the 60 BF16 event/stream rows and
Phase 3F promoted the 60 FP8 event/stream rows. Ascend now classifies all 144
rows as supported performance cases with no deferred reason, so they can be
selected and compared with the same timed operations and data volumes as CUDA.
JSON records every requested feature flag, so the specific
previous/async/allocation combination remains explicit.

### Exhaustive Case Table

| # | Case ID | Suite | Ascend status | Reason |
| ---: | --- | --- | --- | --- |
| 1 | `ep-fp8-align128-bias0-hcopy1-prev0-async0-alloc0` | Performance | Supported |  |
| 2 | `ep-fp8-align128-bias0-hcopy1-prev0-async0-alloc1` | Performance | Supported |  |
| 3 | `ep-fp8-align128-bias0-hcopy1-prev0-async1-alloc0` | Performance | Supported |  |
| 4 | `ep-fp8-align128-bias0-hcopy1-prev0-async1-alloc1` | Performance | Supported |  |
| 5 | `ep-fp8-align128-bias0-hcopy1-prev1-async0-alloc1` | Performance | Supported |  |
| 6 | `ep-fp8-align128-bias0-hcopy1-prev1-async1-alloc1` | Performance | Supported |  |
| 7 | `ep-fp8-align128-bias1-hcopy1-prev0-async0-alloc0` | Performance | Supported |  |
| 8 | `ep-fp8-align128-bias1-hcopy1-prev0-async0-alloc1` | Performance | Supported |  |
| 9 | `ep-fp8-align128-bias1-hcopy1-prev0-async1-alloc0` | Performance | Supported |  |
| 10 | `ep-fp8-align128-bias1-hcopy1-prev0-async1-alloc1` | Performance | Supported |  |
| 11 | `ep-fp8-align128-bias1-hcopy1-prev1-async0-alloc1` | Performance | Supported |  |
| 12 | `ep-fp8-align128-bias1-hcopy1-prev1-async1-alloc1` | Performance | Supported |  |
| 13 | `ep-fp8-align128-bias2-hcopy1-prev0-async0-alloc0` | Performance | Supported |  |
| 14 | `ep-fp8-align128-bias2-hcopy1-prev0-async0-alloc1` | Performance | Supported |  |
| 15 | `ep-fp8-align128-bias2-hcopy1-prev0-async1-alloc0` | Performance | Supported |  |
| 16 | `ep-fp8-align128-bias2-hcopy1-prev0-async1-alloc1` | Performance | Supported |  |
| 17 | `ep-fp8-align128-bias2-hcopy1-prev1-async0-alloc1` | Performance | Supported |  |
| 18 | `ep-fp8-align128-bias2-hcopy1-prev1-async1-alloc1` | Performance | Supported |  |
| 19 | `ep-bf16-align128-bias0-hcopy1-prev0-async0-alloc0` | Performance | Supported |  |
| 20 | `ep-bf16-align128-bias0-hcopy1-prev0-async0-alloc1` | Performance | Supported |  |
| 21 | `ep-bf16-align128-bias0-hcopy1-prev0-async1-alloc0` | Performance | Supported |  |
| 22 | `ep-bf16-align128-bias0-hcopy1-prev0-async1-alloc1` | Performance | Supported |  |
| 23 | `ep-bf16-align128-bias0-hcopy1-prev1-async0-alloc1` | Performance | Supported |  |
| 24 | `ep-bf16-align128-bias0-hcopy1-prev1-async1-alloc1` | Performance | Supported |  |
| 25 | `ep-bf16-align128-bias1-hcopy1-prev0-async0-alloc0` | Performance | Supported |  |
| 26 | `ep-bf16-align128-bias1-hcopy1-prev0-async0-alloc1` | Performance | Supported |  |
| 27 | `ep-bf16-align128-bias1-hcopy1-prev0-async1-alloc0` | Performance | Supported |  |
| 28 | `ep-bf16-align128-bias1-hcopy1-prev0-async1-alloc1` | Performance | Supported |  |
| 29 | `ep-bf16-align128-bias1-hcopy1-prev1-async0-alloc1` | Performance | Supported |  |
| 30 | `ep-bf16-align128-bias1-hcopy1-prev1-async1-alloc1` | Performance | Supported |  |
| 31 | `ep-bf16-align128-bias2-hcopy1-prev0-async0-alloc0` | Performance | Supported |  |
| 32 | `ep-bf16-align128-bias2-hcopy1-prev0-async0-alloc1` | Performance | Supported |  |
| 33 | `ep-bf16-align128-bias2-hcopy1-prev0-async1-alloc0` | Performance | Supported |  |
| 34 | `ep-bf16-align128-bias2-hcopy1-prev0-async1-alloc1` | Performance | Supported |  |
| 35 | `ep-bf16-align128-bias2-hcopy1-prev1-async0-alloc1` | Performance | Supported |  |
| 36 | `ep-bf16-align128-bias2-hcopy1-prev1-async1-alloc1` | Performance | Supported |  |
| 37 | `ep-fp8-align1-bias0-hcopy1-prev0-async0-alloc0` | Performance | Supported |  |
| 38 | `ep-fp8-align1-bias0-hcopy1-prev0-async0-alloc1` | Performance | Supported |  |
| 39 | `ep-fp8-align1-bias0-hcopy1-prev0-async1-alloc0` | Performance | Supported |  |
| 40 | `ep-fp8-align1-bias0-hcopy1-prev0-async1-alloc1` | Performance | Supported |  |
| 41 | `ep-fp8-align1-bias0-hcopy1-prev1-async0-alloc1` | Performance | Supported |  |
| 42 | `ep-fp8-align1-bias0-hcopy1-prev1-async1-alloc1` | Performance | Supported |  |
| 43 | `ep-fp8-align1-bias1-hcopy1-prev0-async0-alloc0` | Performance | Supported |  |
| 44 | `ep-fp8-align1-bias1-hcopy1-prev0-async0-alloc1` | Performance | Supported |  |
| 45 | `ep-fp8-align1-bias1-hcopy1-prev0-async1-alloc0` | Performance | Supported |  |
| 46 | `ep-fp8-align1-bias1-hcopy1-prev0-async1-alloc1` | Performance | Supported |  |
| 47 | `ep-fp8-align1-bias1-hcopy1-prev1-async0-alloc1` | Performance | Supported |  |
| 48 | `ep-fp8-align1-bias1-hcopy1-prev1-async1-alloc1` | Performance | Supported |  |
| 49 | `ep-fp8-align1-bias2-hcopy1-prev0-async0-alloc0` | Performance | Supported |  |
| 50 | `ep-fp8-align1-bias2-hcopy1-prev0-async0-alloc1` | Performance | Supported |  |
| 51 | `ep-fp8-align1-bias2-hcopy1-prev0-async1-alloc0` | Performance | Supported |  |
| 52 | `ep-fp8-align1-bias2-hcopy1-prev0-async1-alloc1` | Performance | Supported |  |
| 53 | `ep-fp8-align1-bias2-hcopy1-prev1-async0-alloc1` | Performance | Supported |  |
| 54 | `ep-fp8-align1-bias2-hcopy1-prev1-async1-alloc1` | Performance | Supported |  |
| 55 | `ep-bf16-align1-bias0-hcopy1-prev0-async0-alloc0` | Performance | Supported |  |
| 56 | `ep-bf16-align1-bias0-hcopy1-prev0-async0-alloc1` | Performance | Supported |  |
| 57 | `ep-bf16-align1-bias0-hcopy1-prev0-async1-alloc0` | Performance | Supported |  |
| 58 | `ep-bf16-align1-bias0-hcopy1-prev0-async1-alloc1` | Performance | Supported |  |
| 59 | `ep-bf16-align1-bias0-hcopy1-prev1-async0-alloc1` | Performance | Supported |  |
| 60 | `ep-bf16-align1-bias0-hcopy1-prev1-async1-alloc1` | Performance | Supported |  |
| 61 | `ep-bf16-align1-bias1-hcopy1-prev0-async0-alloc0` | Performance | Supported |  |
| 62 | `ep-bf16-align1-bias1-hcopy1-prev0-async0-alloc1` | Performance | Supported |  |
| 63 | `ep-bf16-align1-bias1-hcopy1-prev0-async1-alloc0` | Performance | Supported |  |
| 64 | `ep-bf16-align1-bias1-hcopy1-prev0-async1-alloc1` | Performance | Supported |  |
| 65 | `ep-bf16-align1-bias1-hcopy1-prev1-async0-alloc1` | Performance | Supported |  |
| 66 | `ep-bf16-align1-bias1-hcopy1-prev1-async1-alloc1` | Performance | Supported |  |
| 67 | `ep-bf16-align1-bias2-hcopy1-prev0-async0-alloc0` | Performance | Supported |  |
| 68 | `ep-bf16-align1-bias2-hcopy1-prev0-async0-alloc1` | Performance | Supported |  |
| 69 | `ep-bf16-align1-bias2-hcopy1-prev0-async1-alloc0` | Performance | Supported |  |
| 70 | `ep-bf16-align1-bias2-hcopy1-prev0-async1-alloc1` | Performance | Supported |  |
| 71 | `ep-bf16-align1-bias2-hcopy1-prev1-async0-alloc1` | Performance | Supported |  |
| 72 | `ep-bf16-align1-bias2-hcopy1-prev1-async1-alloc1` | Performance | Supported |  |
| 73 | `ep-fp8-align128-bias0-hcopy0-prev0-async0-alloc0` | Performance | Supported |  |
| 74 | `ep-fp8-align128-bias0-hcopy0-prev0-async0-alloc1` | Performance | Supported |  |
| 75 | `ep-fp8-align128-bias0-hcopy0-prev0-async1-alloc0` | Performance | Supported |  |
| 76 | `ep-fp8-align128-bias0-hcopy0-prev0-async1-alloc1` | Performance | Supported |  |
| 77 | `ep-fp8-align128-bias0-hcopy0-prev1-async0-alloc1` | Performance | Supported |  |
| 78 | `ep-fp8-align128-bias0-hcopy0-prev1-async1-alloc1` | Performance | Supported |  |
| 79 | `ep-fp8-align128-bias1-hcopy0-prev0-async0-alloc0` | Performance | Supported |  |
| 80 | `ep-fp8-align128-bias1-hcopy0-prev0-async0-alloc1` | Performance | Supported |  |
| 81 | `ep-fp8-align128-bias1-hcopy0-prev0-async1-alloc0` | Performance | Supported |  |
| 82 | `ep-fp8-align128-bias1-hcopy0-prev0-async1-alloc1` | Performance | Supported |  |
| 83 | `ep-fp8-align128-bias1-hcopy0-prev1-async0-alloc1` | Performance | Supported |  |
| 84 | `ep-fp8-align128-bias1-hcopy0-prev1-async1-alloc1` | Performance | Supported |  |
| 85 | `ep-fp8-align128-bias2-hcopy0-prev0-async0-alloc0` | Performance | Supported |  |
| 86 | `ep-fp8-align128-bias2-hcopy0-prev0-async0-alloc1` | Performance | Supported |  |
| 87 | `ep-fp8-align128-bias2-hcopy0-prev0-async1-alloc0` | Performance | Supported |  |
| 88 | `ep-fp8-align128-bias2-hcopy0-prev0-async1-alloc1` | Performance | Supported |  |
| 89 | `ep-fp8-align128-bias2-hcopy0-prev1-async0-alloc1` | Performance | Supported |  |
| 90 | `ep-fp8-align128-bias2-hcopy0-prev1-async1-alloc1` | Performance | Supported |  |
| 91 | `ep-bf16-align128-bias0-hcopy0-prev0-async0-alloc0` | Performance | Supported |  |
| 92 | `ep-bf16-align128-bias0-hcopy0-prev0-async0-alloc1` | Performance | Supported |  |
| 93 | `ep-bf16-align128-bias0-hcopy0-prev0-async1-alloc0` | Performance | Supported |  |
| 94 | `ep-bf16-align128-bias0-hcopy0-prev0-async1-alloc1` | Performance | Supported |  |
| 95 | `ep-bf16-align128-bias0-hcopy0-prev1-async0-alloc1` | Performance | Supported |  |
| 96 | `ep-bf16-align128-bias0-hcopy0-prev1-async1-alloc1` | Performance | Supported |  |
| 97 | `ep-bf16-align128-bias1-hcopy0-prev0-async0-alloc0` | Performance | Supported |  |
| 98 | `ep-bf16-align128-bias1-hcopy0-prev0-async0-alloc1` | Performance | Supported |  |
| 99 | `ep-bf16-align128-bias1-hcopy0-prev0-async1-alloc0` | Performance | Supported |  |
| 100 | `ep-bf16-align128-bias1-hcopy0-prev0-async1-alloc1` | Performance | Supported |  |
| 101 | `ep-bf16-align128-bias1-hcopy0-prev1-async0-alloc1` | Performance | Supported |  |
| 102 | `ep-bf16-align128-bias1-hcopy0-prev1-async1-alloc1` | Performance | Supported |  |
| 103 | `ep-bf16-align128-bias2-hcopy0-prev0-async0-alloc0` | Performance | Supported |  |
| 104 | `ep-bf16-align128-bias2-hcopy0-prev0-async0-alloc1` | Performance | Supported |  |
| 105 | `ep-bf16-align128-bias2-hcopy0-prev0-async1-alloc0` | Performance | Supported |  |
| 106 | `ep-bf16-align128-bias2-hcopy0-prev0-async1-alloc1` | Performance | Supported |  |
| 107 | `ep-bf16-align128-bias2-hcopy0-prev1-async0-alloc1` | Performance | Supported |  |
| 108 | `ep-bf16-align128-bias2-hcopy0-prev1-async1-alloc1` | Performance | Supported |  |
| 109 | `ep-fp8-align1-bias0-hcopy0-prev0-async0-alloc0` | Performance | Supported |  |
| 110 | `ep-fp8-align1-bias0-hcopy0-prev0-async0-alloc1` | Performance | Supported |  |
| 111 | `ep-fp8-align1-bias0-hcopy0-prev0-async1-alloc0` | Performance | Supported |  |
| 112 | `ep-fp8-align1-bias0-hcopy0-prev0-async1-alloc1` | Performance | Supported |  |
| 113 | `ep-fp8-align1-bias0-hcopy0-prev1-async0-alloc1` | Performance | Supported |  |
| 114 | `ep-fp8-align1-bias0-hcopy0-prev1-async1-alloc1` | Performance | Supported |  |
| 115 | `ep-fp8-align1-bias1-hcopy0-prev0-async0-alloc0` | Performance | Supported |  |
| 116 | `ep-fp8-align1-bias1-hcopy0-prev0-async0-alloc1` | Performance | Supported |  |
| 117 | `ep-fp8-align1-bias1-hcopy0-prev0-async1-alloc0` | Performance | Supported |  |
| 118 | `ep-fp8-align1-bias1-hcopy0-prev0-async1-alloc1` | Performance | Supported |  |
| 119 | `ep-fp8-align1-bias1-hcopy0-prev1-async0-alloc1` | Performance | Supported |  |
| 120 | `ep-fp8-align1-bias1-hcopy0-prev1-async1-alloc1` | Performance | Supported |  |
| 121 | `ep-fp8-align1-bias2-hcopy0-prev0-async0-alloc0` | Performance | Supported |  |
| 122 | `ep-fp8-align1-bias2-hcopy0-prev0-async0-alloc1` | Performance | Supported |  |
| 123 | `ep-fp8-align1-bias2-hcopy0-prev0-async1-alloc0` | Performance | Supported |  |
| 124 | `ep-fp8-align1-bias2-hcopy0-prev0-async1-alloc1` | Performance | Supported |  |
| 125 | `ep-fp8-align1-bias2-hcopy0-prev1-async0-alloc1` | Performance | Supported |  |
| 126 | `ep-fp8-align1-bias2-hcopy0-prev1-async1-alloc1` | Performance | Supported |  |
| 127 | `ep-bf16-align1-bias0-hcopy0-prev0-async0-alloc0` | Performance | Supported |  |
| 128 | `ep-bf16-align1-bias0-hcopy0-prev0-async0-alloc1` | Performance | Supported |  |
| 129 | `ep-bf16-align1-bias0-hcopy0-prev0-async1-alloc0` | Performance | Supported |  |
| 130 | `ep-bf16-align1-bias0-hcopy0-prev0-async1-alloc1` | Performance | Supported |  |
| 131 | `ep-bf16-align1-bias0-hcopy0-prev1-async0-alloc1` | Performance | Supported |  |
| 132 | `ep-bf16-align1-bias0-hcopy0-prev1-async1-alloc1` | Performance | Supported |  |
| 133 | `ep-bf16-align1-bias1-hcopy0-prev0-async0-alloc0` | Performance | Supported |  |
| 134 | `ep-bf16-align1-bias1-hcopy0-prev0-async0-alloc1` | Performance | Supported |  |
| 135 | `ep-bf16-align1-bias1-hcopy0-prev0-async1-alloc0` | Performance | Supported |  |
| 136 | `ep-bf16-align1-bias1-hcopy0-prev0-async1-alloc1` | Performance | Supported |  |
| 137 | `ep-bf16-align1-bias1-hcopy0-prev1-async0-alloc1` | Performance | Supported |  |
| 138 | `ep-bf16-align1-bias1-hcopy0-prev1-async1-alloc1` | Performance | Supported |  |
| 139 | `ep-bf16-align1-bias2-hcopy0-prev0-async0-alloc0` | Performance | Supported |  |
| 140 | `ep-bf16-align1-bias2-hcopy0-prev0-async0-alloc1` | Performance | Supported |  |
| 141 | `ep-bf16-align1-bias2-hcopy0-prev0-async1-alloc0` | Performance | Supported |  |
| 142 | `ep-bf16-align1-bias2-hcopy0-prev0-async1-alloc1` | Performance | Supported |  |
| 143 | `ep-bf16-align1-bias2-hcopy0-prev1-async0-alloc1` | Performance | Supported |  |
| 144 | `ep-bf16-align1-bias2-hcopy0-prev1-async1-alloc1` | Performance | Supported |  |

### Operations Per Supported Case

| Operation | Correctness | Performance | Notes |
| --- | --- | --- | --- |
| Normal dispatch | Yes | Yes | Same routing manifest |
| Expanded dispatch | Yes | Yes | FP8 uses upstream column-major scales; Ascend leaves the meaningless BF16 scale-layout flag false |
| Cached dispatch | Yes | Yes | Reuses validated handle |
| Cached expanded zero padding | Yes | No | Upstream has no separate timing result |
| Normal combine | Yes | Yes | Same weights and bias count |
| Reduced/expanded combine | Yes | Yes | Same multiple-reduction setting |

The 144 supported performance modes each produce five primary operation
records, for 720 records per default complete workload configuration. The 24
synchronous baseline cases account for 120 records; the 120 BF16/FP8
event/stream cases account for the other 600.

## Workload Parity

### Default Configuration

The parity default copies `tests/elastic/test_ep.py`:

| Parameter | Value |
| --- | --- |
| Local processes | 8 |
| Maximum tokens per rank | 4096 |
| Tokens on rank `r` | `max(1, 4096 - r)` |
| Hidden width | 7168 |
| Top-k | 6 |
| Experts | 256 |
| Seed | 0 |
| Unbalanced ratio | 1.0 |
| Precise unbalanced ratio | false |
| Masked ratio | 0.0 |
| Allow multiple reduction | true |
| Allow hybrid mode | false for comparable profile |
| CPU synchronization | true |

CLI overrides remain available for the same workload parameters.

### Manifest Contents

The CPU-side manifest stores or deterministically derives:

- schema version and seed;
- rank count and per-rank token counts;
- hidden, expert, top-k, alignment, dtype, and mask settings;
- exact `topk_idx` routing values;
- exact top-k weights when enabled;
- bias count and tensor shapes;
- per-rank and per-expert send/receive summaries;
- logical payload and metadata byte counts;
- a canonical SHA-256 workload fingerprint.

Large payload tensors and bias values are generated on each platform. Their
values may differ, but dtype, shape, and byte count must match. Routing and
weights must be identical because they determine traffic.

The existing CUDA test keeps its default random path unchanged. An opt-in
parity profile loads the shared manifest and emits the parity JSON schema.

## Timing Protocol

The canonical comparison metric is device elapsed time around the public
`ElasticBuffer` operation. CUDA uses CUDA events and Ascend uses NPU events.
Python wall time is also recorded to expose synchronous host overhead.

| Setting | Default |
| --- | ---: |
| Untimed preparation call | 1 |
| Warmup iterations | 30 |
| Measured iterations | 30 |
| Rank alignment | Before every measured sample, outside timing |
| Device synchronization | Before start and after end event |
| L2 flush | Disabled in canonical parity mode |

The 1+30 warmup shape matches the effective preparation and profiler warmup in
`bench_kineto`. L2 flush is disabled for both parity backends because the
current helper is CUDA-specific and there is no proven equivalent Ascend cache
flush contract.

Each timed operation record contains:

- raw device elapsed samples in seconds;
- raw wall-time samples in seconds;
- minimum, mean, p50, p95, and maximum;
- logical bytes and logical GB/s using decimal `1e9`;
- rank-local send and receive byte summaries;
- backend resource values, including CUDA SM/QP or Ascend fixed `1/0`;
- optional profiler kernel-stage diagnostics.

Kernel-stage diagnostics are not a comparison gate. CUDA main/copy epilogues
and Ascend AOT kernels may have different decomposition and names.

## Logical Bandwidth

Dispatch and combine byte calculations reuse the formulas in
`tests/elastic/test_ep.py`, including:

- scale-up receive bytes;
- scale-out send bytes when scale-out is supported;
- local-traffic inclusion or exclusion;
- dispatch payload, top-k index, and weight bytes;
- expanded metadata and expanded-token bytes;
- combine payload, bias, reduction-read, and reduction-write bytes.

Only records with matching workload fingerprint, case ID, operation ID,
logical-byte formula version, and world size may be compared.

## Eight-Rank Offline Automation

### Purpose And Scope

The automation layer runs the existing parity benchmark without changing its
case generation, operation sequence, correctness checks, timing protocol, or
logical-byte formulas. It supports exactly two single-host backends:

- eight NVIDIA H800 GPUs using the CUDA parity runner; and
- eight Ascend 950 NPUs using the Ascend parity runner.

It does not submit cluster jobs, install dependencies, build DeepEP, transfer
files between hosts, call an AI model, or require network access. The caller
prepares the backend environment and transfers the workload manifest and JSON
reports when the two runs occur on different hosts. Scale-out and hybrid
routing remain outside this automation profile.

The automation inventory is all 144 case IDs on both backends. The current
Ascend inventory satisfies the strict readiness preflight with 144 supported
rows and zero deferred rows. The preflight remains a regression guard: the
Ascend launcher must fail rather than emit a partial canonical result or claim
unsupported performance if the inventory drifts from `144/144/0`.

### Profiles

The automation exposes only `canonical` and `smoke`; individual workload-size
overrides are intentionally absent from this entry point.

| Setting | `canonical` | `smoke` |
| --- | ---: | ---: |
| Local ranks | 8 | 8 |
| Maximum tokens per rank | 4096 | 16 |
| Hidden width | 7168 | 128 |
| Top-k | 6 | 2 |
| Experts | 256 | 8 |
| Seed | 0 | 0 |
| Unbalanced ratio | 1.0 | 1.0 |
| Precise unbalanced ratio | false | false |
| Masked ratio | 0.0 | 0.0 |
| Allow multiple reduction | true | true |
| Warmups per operation | 30 | 1 |
| Measured iterations per operation | 30 | 1 |
| Selected cases | all 144 | all 144 |
| Intended use | Formal H800/Ascend comparison | Automation validation only |

`canonical` copies the original `tests/elastic/test_ep.py` performance
defaults. Neither backend may reduce its data size dynamically. `smoke` keeps
the complete case and operation matrix but uses bounded data and timing counts
to validate launching, JSON collection, comparison, and Markdown generation.
Every smoke artifact and Markdown report is labeled non-canonical and must not
be presented as formal performance evidence.

### Run Interface

The cross-platform entry point is:

```bash
python3 tests/benchmark/run_ep.py \
  --backend cuda \
  --profile canonical \
  --output-dir results/h800-canonical
```

The first run writes a deterministic workload manifest. A run on the other
backend consumes that exact file:

```bash
python3 tests/benchmark/run_ep.py \
  --backend ascend \
  --profile canonical \
  --workload-manifest results/h800-canonical/workload.json \
  --output-dir results/ascend-canonical
```

`--backend` accepts only `cuda` or `ascend`; `--profile` accepts only
`canonical` or `smoke`. The default profile is `smoke` so an omitted profile
cannot accidentally start a formal, long-running benchmark. The output
directory must not be the repository root and must contain no completed
benchmark artifacts unless the caller explicitly selects a new directory.

The CUDA adapter launches `tests/elastic/test_ep.py` with parity mode and
`--num-processes 8`. It passes all 144 canonical case IDs, retains automatic
SM/QP selection, and validates that the emitted report has platform `cuda`,
world size eight, and an H800 device name. The existing upstream profile and
its defaults remain unchanged.

The Ascend adapter launches `tests/ascend/benchmark/bench_ep.py` through the
current interpreter's `torch.distributed.run` module with `--standalone` and
`--nproc-per-node=8`. Before importing runtime libraries or reserving benchmark
output, it requires the Ascend inventory to contain 144 supported rows. It
retains the Ascend fixed `num_sms=1`, `num_qps=0`, and single-host pure-scale-up
behavior.

Both adapters use argument arrays rather than interpolated shell commands,
inherit the prepared environment, and stream combined stdout/stderr to the
terminal and `run.log`. A nonzero child exit code is the automation exit code.

### Run Artifacts

Each successful run completes this directory:

```text
output-dir/
|-- workload.json
|-- benchmark.json
|-- benchmark.md
`-- run.log
```

`workload.json` is the exact shared manifest. When `--workload-manifest` is
provided, the automation validates its profile values and copies its bytes to
the output directory. The backend writes to a staging JSON path. Only after
validation does the automation atomically publish it as `benchmark.json`, the
machine-readable source of truth. `benchmark.md` is atomically generated from
that JSON; it is never parsed as comparison input. `run.log` records the exact
argument vector, selected profile, inherited Git commit, start/end timestamps,
and child output without serializing environment secrets.

After the backend exits successfully, the automation rejects the run unless:

- the report schema and formula versions are supported;
- platform, eight-rank world size, workload, fingerprint, execution protocol,
  warmups, iterations, and aggregation rules match the selected profile and
  manifest;
- all 144 expected case IDs appear once in canonical enumeration order;
- all cases passed with no failures; and
- every case contains the same five expected timed operation IDs with positive
  finite latency and logical-bandwidth metrics.

Failure preserves `run.log`, `workload.json`, the staging report if one was
produced, and child diagnostics, but it never publishes `benchmark.json` or
`benchmark.md`.

### Offline Comparison

Comparison consumes the two first-stage JSON reports, not their Markdown:

```bash
python3 tests/benchmark/compare_ep.py \
  --cuda results/h800-canonical/benchmark.json \
  --ascend results/ascend-canonical/benchmark.json \
  --output comparison.md
```

The command runs on a machine with only Python and the repository. It does not
import CUDA, Torch-NPU, DeepEP runtime modules, or model services. It rejects
reports unless both are the same profile and match on schema/formula version,
world size, workload fingerprint, execution protocol, timing counts and
aggregation, complete 144-case sequence, five operation IDs per case, and
logical bytes for every matched operation. Profile identity is derived from
the exact workload, execution-protocol, and timing values in each JSON report,
not from its filename or Markdown text. The comparison rejects an output path
that resolves to either input report, including an existing filesystem alias.

The comparison Markdown starts with provenance and workload tables, followed
by exactly 720 detail rows in canonical case order and fixed operation order.
Each row represents one operation from one case:

| Column | Definition |
| --- | --- |
| Case | Stable case ID |
| Operation | Stable operation ID |
| H800 mean/p50/p95 | Device latency statistics in microseconds |
| H800 logical GB/s | Logical bytes divided by H800 mean device latency |
| Ascend mean/p50/p95 | Device latency statistics in microseconds |
| Ascend logical GB/s | Logical bytes divided by Ascend mean device latency |
| Latency Ascend/H800 | Ascend mean divided by H800 mean; lower is better |
| Bandwidth Ascend/H800 | Ascend GB/s divided by H800 GB/s; higher is better |

The per-backend `benchmark.md` uses the same case and operation ordering but
contains only that backend's mean, p50, p95, and logical GB/s. Values use three
decimal places, Markdown-sensitive text is escaped, and non-finite or
nonpositive metrics are rejected rather than rendered.

### Automation Testing And Acceptance

Host-only tests use small complete JSON fixtures and a fake child launcher;
they do not require a GPU or NPU. They prove profile argument expansion,
manifest copying, safe subprocess arguments, H800 and Ascend readiness gates,
atomic artifact behavior, report validation, Markdown escaping and ordering,
ratio calculations, and rejection of every comparison identity mismatch.

Hardware acceptance is separate and serialized per environment:

1. An eight-rank H800 smoke run passes all 144 cases and writes 720 operation
   records plus all four artifacts.
2. An eight-rank Ascend smoke run passes the same 144 cases and writes the same
   operation-key sequence.
3. Offline smoke comparison produces 720 rows and is labeled non-canonical.
4. Formal H800 and Ascend canonical runs reuse one manifest and each pass all
   144 cases with 720 operation records.
5. Canonical comparison validates every identity field before atomically
   writing the final Markdown report.

## Components

| File | Responsibility |
| --- | --- |
| `tests/utils/ep_benchmark_core.py` | Shared operation sequence, correctness checks, logical-byte formulas, timing protocol |
| `tests/utils/ep_benchmark_manifest.py` | Shared case axes, manifest schema, deterministic routing, fingerprints |
| `tests/ascend/benchmark/workloads.py` | Ascend capability classification and operation expansion |
| `tests/ascend/benchmark/timing.py` | NPU event and wall-time sampling, statistics |
| `tests/ascend/benchmark/report.py` | JSON serialization and schema validation |
| `tests/ascend/benchmark/bench_ep.py` | HCCL setup, correctness, warmup, measurement, cleanup, CLI |
| `tests/ascend/benchmark/compare.py` | Match GPU/NPU records and produce comparison tables |
| `tests/ascend/benchmark/README.md` | Build, TaskQueue, torchrun, output, limitations |
| `tests/ascend/test_benchmark_contract.py` | Host-only matrix, manifest, report, statistics, CLI tests |
| `tests/elastic/test_ep.py` | Opt-in parity manifest/profile; existing default remains unchanged |
| `tests/benchmark/run_ep.py` | Backend-neutral eight-rank profile expansion, launch, validation, and artifact orchestration |
| `tests/benchmark/report_markdown.py` | Deterministic single-backend and comparison Markdown rendering |
| `tests/benchmark/compare_ep.py` | Runtime-free offline comparison CLI |
| `tests/benchmark/test_automation.py` | Host-only automation, validation, and Markdown contracts |

The shared core and manifest modules live under `tests/utils`. The CUDA parity
profile imports those modules and currently reuses the Ascend report, timing,
and matrix-orchestration adapters; the Ascend runner supplies NPU-specific
materialization and event timing.

## CLI Contract

Representative commands:

```bash
# Print all 144 cases without importing torch_npu
python3 tests/ascend/benchmark/bench_ep.py --list-cases --suite all

# Two-rank Ascend benchmark inside an allocated NPU task
torchrun --standalone --nproc-per-node=2 \
  tests/ascend/benchmark/bench_ep.py --output results/ascend-ep2.json

# GPU parity profile
python3 tests/elastic/test_ep.py --benchmark-profile parity \
  --workload-manifest results/ep8-workload.json \
  --benchmark-json results/cuda-ep8.json

# Compare matching records
python3 tests/ascend/benchmark/compare.py \
  results/cuda-ep8.json results/ascend-ep8.json
```

The `--suite` argument is valid with `--list-cases`; default benchmark execution
uses all 144 currently supported performance cases. Explicit `--cases`
selection accepts supported performance cases before runtime dependency
loading.

## Reporting

### Terminal

On success, rank 0 prints the output path and passed-case count after atomically
writing the complete JSON report. Runtime failures are raised through the
distributed launcher and do not produce a replacement report.

Case inventory is a separate listing operation. Both `--list-cases --suite all`
and `--suite performance` show all 144 supported rows (24 synchronous baseline
plus 120 BF16/FP8 event/stream), while `--suite functional` shows zero rows.
Listing does not initialize `torch_npu` or access an NPU.

### JSON

The top level contains:

```text
schema_version
formula_version
generated_at
git_commit
platform
device
world_size
workload
workload_fingerprint
execution_protocol
timing_protocol
case_summary
cases
failures
```

Benchmark reports use schema version 2. `execution_protocol` is exactly:

```json
{"allow_multiple_reduction": 0}
```

or the same object with value `1`, populated from the backend's actual CLI
argument. This setting is execution identity, not routing identity or timing,
so it is deliberately absent from the workload fingerprint and
`timing_protocol`. The fixed `canonical` and `smoke` automation profiles require
value `1`; schema-v1 reports and direct-run reports with value `0` are rejected
by automation identification, validation, and comparison. No schema-v1
migration is provided before merge.

`git_commit` records the repository `HEAD` that generated the report. Source
archives without Git metadata use the explicit value `unknown`.

The current runner emits JSON only after every selected case passes. Emitted
reports therefore have `case_summary.failed=0` and an empty `failures` list;
these schema fields are reserved for readers and future incremental reporting.
On failure, the process exits nonzero without replacing `--output`, so callers
must use the task exit status and report provenance rather than treating a
pre-existing file as the current run.

The report contains exactly the selected supported cases. The default complete
run therefore contains 144 passed cases and 720 operation records. Each row's
case ID and mode fields preserve its suite inputs. No current inventory row is
deferred.

## Error Handling

- Any unsupported or deferred case introduced by a future capability change is
  rejected from benchmark selection with its suite and stable reason.
- Every case in an emitted report is `passed`; a case failure aborts the run and
  prevents report replacement.
- A failure on any rank is aggregated before the next collective boundary.
- Peer failure produces a bounded, rank-qualified error.
- Correctness failure prevents timing for that case.
- Buffer destruction precedes process-group destruction.
- JSON is written atomically after all selected cases complete and rank results
  are aggregated.
- Comparison rejects mismatched fingerprints, case IDs, formula versions,
  world sizes, execution protocols, timing counts/aggregation, logical bytes,
  or operation IDs rather than displaying misleading ratios. CUDA and NPU
  event timer names are platform-specific and are not required to match.

## Testing

Host-only TDD coverage must prove:

- exactly 144 stable case IDs are generated;
- exactly 12 cases are synchronous BF16 performance, 12 are synchronous FP8
  performance, 60 are BF16 async/event performance, and 60 are FP8 async/event
  performance;
- all 144 cases are supported performance cases with an empty reason;
- the 24 synchronous performance cases are event-free and allocation-free;
- the 120 BF16/FP8 async/event performance cases cover every previous-event,
  async, and communication-allocation combination;
- each supported case expands to the required six correctness and five timed
  operations;
- identical manifest inputs produce identical fingerprints;
- route summaries and logical bytes match across CUDA and Ascend profiles;
- percentile and GB/s calculations are correct;
- default JSON contains exactly 144 passed cases and 720 operation records;
- comparison rejects incompatible inputs;
- all/performance/functional case listing works without importing `torch_npu`.

NPU acceptance must cover:

- two-rank full 144-case matrix;
- four-rank 144-case smoke;
- eight-rank 144-case smoke;
- bounded peer failure and cleanup;
- JSON production and comparison against a matching CUDA parity result.

Full four- and eight-rank correctness qualification is not inferred from smoke
coverage. Reports must record the qualification level.

## Acceptance Criteria

1. The standalone Ascend benchmark lists and classifies all 144 upstream modes.
2. The default report contains exactly 144 passed performance cases: 24
   synchronous baseline and 120 BF16/FP8 event/stream.
3. The inventory contains 144 supported performance cases, zero deferred
   cases, and no current deferred reason.
4. The 144 supported cases run the required operation variants and emit exactly
   720 timing records.
5. GPU and Ascend parity records share workload fingerprints and logical bytes.
6. Canonical latency and bandwidth statistics use the same timing protocol.
7. Existing `tests/elastic/test_ep.py` behavior is unchanged without parity flags.
8. CUDA and Ascend runners call the same shared mode, workload, operation,
   correctness, and logical-byte implementations.
9. Host-only tests and current Ascend platform/build contracts pass.
10. Two-rank NPU runtime acceptance passes before performance claims are made.
11. Results never imply support for FP8 combine, hybrid routing, or scale-out.
